# Common configuration shared across all environments
# These are safe defaults; no secrets or environment-specific values.
# Override any of these in .env.local (for local) or via ECS env vars (for stage/prod).

# Core
SERVICE_NAME=pulse-backend

# HTTP server
APP_HOST=0.0.0.0
APP_PORT=8000

# Logging / tracing
LOG_LEVEL=INFO
TRACING_ENABLED=false

# Database (non-secret defaults)
PULSE_DB_PORT=5432
PULSE_DB_USER=pulse
PULSE_DB_NAME=pulse

# Internal service URLs (REQUIRED)
# For monorepo deployment (single process):
#   PULSE_API_BASE_URL=http://localhost:8000/pulse
# For separate deployments:
#   PULSE_API_BASE_URL=http://pulse-service:8001
# PULSE_API_BASE_URL=

