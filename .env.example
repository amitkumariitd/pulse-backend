# Example environment configuration for pulse-backend
# Copy this to .env.local and customize for your local development.
# See doc/standards/config.md for full rules.

# --- Core (required) ---
ENVIRONMENT=local
SERVICE_NAME=pulse-backend

# --- HTTP server (required) ---
APP_HOST=0.0.0.0
APP_PORT=8000

# --- Logging (required) ---
LOG_LEVEL=INFO
TRACING_ENABLED=false

# --- Database (required) ---
# All fields are required; defaults for port/user/name are in .env.common
PULSE_DB_HOST=localhost
PULSE_DB_PORT=5432
PULSE_DB_USER=pulse
PULSE_DB_PASSWORD=changeme
PULSE_DB_NAME=pulse

# --- Internal service URLs (optional) ---
# Uncomment if you need to override service URLs
# GAPI_BASE_URL=http://localhost:8000/gapi
# PULSE_API_BASE_URL=http://localhost:8001

